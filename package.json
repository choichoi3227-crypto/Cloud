{
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/engine.js && node --check src/platform.js && node --check src/worker.js",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
