const productData={wordpress:{title:'워드프레스 호스팅',desc:'php-wasm 기반의 빠른 워드프레스 실행 환경입니다. 데이터베이스와 스토리지는 별도 상품으로 선택합니다.',chips:['라이트 $14','스탠다드 $29','인텔리전트 $42','임페리얼 $59','듀플렉스 $76','무제한 트래픽','자동 DNS','자동 FTP']},cloudpressdb:{title:'CloudPressDB',desc:'워드프레스 전용 NoSQL, 범용 NoSQL, 범용 SQL 유형을 제공하는 자체 서버리스 데이터베이스입니다.',chips:['월 $10','워드프레스 NoSQL','범용 NoSQL','범용 SQL','KV API','SQL API']},cp3:{title:'CP3',desc:'R2/S3를 쓰지 않는 자체 스토리지입니다. 기본 50GB를 포함하고 초과 사용량만 과금합니다.',chips:['기본 $10','50GB 포함','초과 1GB당 $0.5','무결성 해시']},static:{title:'정적 사이트',desc:'HTML, CSS, JS 파일을 직접 작성해 배포하는 가벼운 웹사이트 호스팅입니다.',chips:['파일 작성','캐시','빠른 배포']},php:{title:'PHP 사이트',desc:'php-wasm 격리 런타임으로 일반 PHP 사이트를 실행합니다.',chips:['php-wasm','월 $12','격리 실행']}};
const routeTitles={'/bridge/index.html':'빠른 워드프레스와 웹서비스를 위한 클라우드','/bridge/feature.html':'성능, 보안, 운영 자동화','/bridge/about.html':'클라우드프레스 소개','/bridge/products.html':'상품 안내','/bridge/notice.html':'공지사항'};
function hostUrl(prefix,path){const host=location.hostname; if(host==='localhost'||host==='127.0.0.1'||host.endsWith('.workers.dev'))return path; const parts=host.split('.'); if(parts.length<2)return path; return `${location.protocol}//${prefix}.${parts.slice(1).join('.')}${path}`}
function nav(){return `<nav class="cp-nav"><a class="brand" href="${hostUrl('bridge','/index')}">클라우드프레스</a><div class="links"><a href="${hostUrl('bridge','/feature')}">기능</a><a href="${hostUrl('bridge','/products')}">상품</a><a href="${hostUrl('bridge','/about')}">소개</a><a href="${hostUrl('bridge','/notice')}">공지</a><a href="${hostUrl('bridge-console','/dashboard')}">콘솔</a><a href="${hostUrl('sso','/login')}">로그인</a><a class="btn primary" href="${hostUrl('sso','/signup')}">가입</a></div></nav>`}
function faq(){return `<section class="faq"><h2>자주 묻는 질문</h2>${[['Durable Objects를 사용하나요?','아니요. 설정을 단순화하기 위해 사용하지 않습니다.'],['구매 URL 형식은 무엇인가요?','/{상품명}/cart/{청구번호} 형식으로 생성됩니다.'],['워드프레스 호스팅에 DB와 스토리지가 포함되나요?','아니요. CloudPressDB와 CP3는 별도 상품으로 주문합니다.']].map(([q,a])=>`<details><summary>${q}</summary><p>${a}</p></details>`).join('')}</section>`}
function productCards(){return `<section class="grid">${Object.entries(productData).map(([slug,p])=>`<article class="card" id="${slug}"><h3>${p.title}</h3><p>${p.desc}</p><p>${p.chips.map(c=>`<span class="pill">${c}</span>`).join('')}</p><button class="btn primary" onclick="buy('${slug}')">구매하기</button></article>`).join('')}</section>`}
function landing(page){const isProducts=page.includes('products'); const isNotice=page.includes('notice'); const isAbout=page.includes('about'); const isFeature=page.includes('feature'); return `${nav()}${isProducts?`<div class="subnav">${Object.keys(productData).map(k=>`<a class="btn" href="#${k}">${productData[k].title}</a>`).join('')}</div>`:''}<section class="hero"><h1>${routeTitles[page]||'클라우드프레스'}</h1><p>${heroCopy(page)}</p><p><a class="btn primary" href="${hostUrl('bridge','/products')}">상품 보기</a> <a class="btn" href="${hostUrl('sso','/signup')}">가입하기</a></p></section>${isProducts?productCards():isNotice?notices():isAbout?aboutContent():isFeature?featureContent():homeContent()}${faq()}`}
function heroCopy(page){if(page.includes('about'))return '클라우드프레스는 워드프레스와 일반 웹서비스를 빠르고 안정적으로 운영하기 위한 호스팅 플랫폼입니다.'; if(page.includes('feature'))return '빠른 실행, 명확한 상품 분리, 보안 중심 인증, 관리자 운영 도구를 제공합니다.'; if(page.includes('notice'))return '서비스 점검, 기능 업데이트, 장애 안내를 확인할 수 있습니다.'; if(page.includes('products'))return '필요한 상품을 선택하고 청구번호 기반 구매 흐름으로 진행합니다.'; return '워드프레스 호스팅, 데이터베이스, 스토리지를 분리해 필요한 만큼 조합하는 클라우드입니다.'}
function homeContent(){return `<section class="grid"><article class="card"><h3>워드프레스에 최적화</h3><p>php-wasm 실행과 CloudPressDB NoSQL 조합으로 빠른 응답을 목표로 합니다.</p></article><article class="card"><h3>분리된 상품 구조</h3><p>호스팅, 데이터베이스, 스토리지를 독립 주문으로 관리해 비용과 운영 범위를 명확히 합니다.</p></article></section>`}
function aboutContent(){return `<section class="grid"><article class="card"><h3>우리가 만드는 것</h3><p>클라우드프레스는 소규모 사이트부터 성장형 워드프레스까지 운영할 수 있는 자체 호스팅 서비스입니다.</p></article><article class="card"><h3>운영 원칙</h3><p>도메인별 역할 분리, 관리자 권한 분리, 보안 헤더, 감사 로그를 기본으로 둡니다.</p></article></section>`}
function featureContent(){return `<section class="grid"><article class="card"><h3>자동화</h3><p>자동 DNS, 자동 FTP, CDN 연결을 관리자가 콘솔에서 운영할 수 있도록 설계했습니다.</p></article><article class="card"><h3>보안</h3><p>SSO 전용 인증 경로, HttpOnly 세션, 관리자 전용 API로 접근을 나눕니다.</p></article></section>`}
function notices(){return `<section class="grid"><article class="card"><h3>서비스 안내</h3><p>정식 공지는 관리자 콘솔에서 작성되며 이 영역에 게시됩니다.</p></article></section>`}
function sso(page){const signup=page.includes('signup'), lost=page.includes('lost'); const params=new URLSearchParams(location.search); const signupSuccess=params.get('signup')==='success'&&!signup&&!lost; return `${nav()}<form class="form" onsubmit="return ${lost?'lostPassword':signup?'signup':'login'}(event)"><h1>${lost?'비밀번호 찾기':signup?'회원가입':'로그인'}</h1>${signupSuccess?'<p class="form-success" style="color:#16a34a">가입이 완료되었습니다. 로그인해 주세요.</p>':''}<input name="email" type="email" placeholder="이메일" required>${signup?'<input name="name" placeholder="이름" required><input name="address" placeholder="주소" required>':''}${lost?'':'<input name="password" type="password" placeholder="비밀번호" required>'}<p class="form-error" style="color:#dc2626"></p><button class="btn primary">전송</button><p>소셜 로그인 정보는 Worker secret으로 연결됩니다.</p><pre class="code" id="out"></pre></form>`}
function consolePage(page){const labels={'dashboard':'대시보드','instances':'인스턴스','instance-detail':'인스턴스 상세','payments':'결제','billing':'청구','accounts':'계정','admin':'관리자','admin/db':'DB 관리','admin/storage':'스토리지 관리','admin/users':'사용자','admin/orders':'주문','admin/settings':'설정'}; const name=page.replace(/^\/admin\//,'admin/').split('/').pop().replace('.html',''); return `<div class="layout"><aside class="side"><h2>클라우드프레스 콘솔</h2>${Object.entries(labels).map(([path,label])=>`<a href="/${path}">${label}</a>`).join('')}</aside><main class="main"><h1>${labels[name]||name}</h1><div class="notice">로그인된 사용자만 접근할 수 있는 운영 콘솔입니다.</div><section class="grid"><article class="card"><h3>빠른 작업</h3><button class="btn primary" onclick="createInstance()">워드프레스 생성</button> <button class="btn" onclick="loadInstances()">인스턴스 조회</button><pre class="code" id="consoleOut"></pre></article><article class="card"><h3>CloudPressDB</h3><button class="btn" onclick="kvDemo()">KV 저장</button> <button class="btn" onclick="sqlDemo()">SQL 실행</button></article><article class="card"><h3>CP3</h3><button class="btn" onclick="cp3Demo()">오브젝트 저장</button></article><article class="card"><h3>관리자 운영</h3><button class="btn" onclick="adminDb()">DB 확인</button> <button class="btn" onclick="adminStorage()">스토리지 확인</button><p>관리자는 모든 상품을 결제 없이 승인 상태로 이용합니다.</p></article></section></main></div>`}
function cart(page){const parts=page.split('/');return `${nav()}<section class="hero"><h1>청구서 확인</h1><p>상품: ${parts[1]||''} / 청구번호: ${parts[3]||''}</p><a class="btn primary" href="${hostUrl('bridge-console','/billing')}">청구 관리로 이동</a></section>`}
async function post(path,body){const r=await fetch(path,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});let data;try{data=await r.json()}catch{data={error:'서버 응답을 해석할 수 없습니다.'}}return {ok:r.ok,status:r.status,data}}function show(v){(document.getElementById('out')||document.getElementById('consoleOut')).textContent=JSON.stringify(v,null,2)}
const errorMessages={'invalid credentials':'이메일 또는 비밀번호가 올바르지 않습니다.','email already exists':'이미 가입된 이메일입니다.','email is required':'이메일을 입력해 주세요.','name is required':'이름을 입력해 주세요.','address is required':'주소를 입력해 주세요.','password is required':'비밀번호를 입력해 주세요.'};
function friendlyError(msg){return errorMessages[msg]||msg||'요청을 처리하지 못했습니다. 다시 시도해 주세요.'}
function formError(form){let el=form.querySelector('.form-error');if(!el){el=document.createElement('p');el.className='form-error';el.style.color='#dc2626';form.insertBefore(el,form.querySelector('#out'));}return el}
async function signup(e){e.preventDefault();const form=e.target;const errEl=formError(form);errEl.textContent='';const submitBtn=form.querySelector('button');submitBtn.disabled=true;try{const {ok,data}=await post('/api/auth/signup',Object.fromEntries(new FormData(form)));if(ok){location.href=hostUrl('sso','/login')+'?signup=success'}else{errEl.textContent=friendlyError(data.error)}}finally{submitBtn.disabled=false}return false}
async function login(e){e.preventDefault();const form=e.target;const errEl=formError(form);errEl.textContent='';const submitBtn=form.querySelector('button');submitBtn.disabled=true;try{const {ok,data}=await post('/api/auth/login',Object.fromEntries(new FormData(form)));if(ok){const params=new URLSearchParams(location.search);const next=params.get('next');location.href=next?`${location.protocol}//${location.hostname.replace(/^sso\./,'bridge-console.')}${next}`:hostUrl('bridge-console','/dashboard')}else{errEl.textContent=friendlyError(data.error)}}finally{submitBtn.disabled=false}return false}
async function lostPassword(e){e.preventDefault();const form=e.target;const errEl=formError(form);errEl.textContent='';const submitBtn=form.querySelector('button');submitBtn.disabled=true;try{const {ok,data}=await post('/api/auth/lost-password',Object.fromEntries(new FormData(form)));if(ok){errEl.style.color='#16a34a';errEl.textContent='비밀번호 재설정 메일을 발송했습니다. (개발 환경 임시 비밀번호: '+(data.developmentTempPassword||'발송됨')+')'}else{errEl.style.color='#dc2626';errEl.textContent=friendlyError(data.error)}}finally{submitBtn.disabled=false}return false}
async function adminDb(){const r=await fetch('/api/admin/db');show(await r.json())} async function adminStorage(){const r=await fetch('/api/admin/storage');show(await r.json())} async function buy(product){const {ok,data}=await post('/api/orders',{product,plan:'lite',gb:50}); if(ok){location.href=data.cartPath}else if(data.error==='authentication required'){location.href=hostUrl('sso','/login')}else{show(data)}} async function createInstance(){const {data}=await post('/api/instances',{type:'wordpress',name:'wp-production'});show(data)} async function loadInstances(){const r=await fetch('/api/instances');show(await r.json())} async function kvDemo(){const {data}=await post('/api/cloudpressdb/kv',{key:'wp:option:siteurl',value:location.origin});show(data)} async function sqlDemo(){await post('/api/cloudpressdb/sql',{sql:'create table posts (id,title)'});const {data}=await post('/api/cloudpressdb/sql',{sql:'insert into posts (id,title) values (?,?)',params:['1','클라우드프레스 예제']});show(data)} async function cp3Demo(){const {data}=await post('/api/cp3/objects',{name:'hello.txt',data:'CloudPress CP3 object'});show(data)}
const page=document.getElementById('app').dataset.page;document.getElementById('app').innerHTML=page.includes('/sso/')?sso(page):page.includes('/console/')||page.includes('/admin/')?consolePage(page):page.includes('/cart/')?cart(page):landing(page);
